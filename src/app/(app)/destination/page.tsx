import { redirect } from "next/navigation";

/**
 * Destination Selection (Page 5)
 *
 * Temporary redirect to the existing MVP implementation.
 */
export default function DestinationPage() {
  redirect("/journey/destination");
}
